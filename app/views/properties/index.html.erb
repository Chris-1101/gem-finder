<h1>Welcome To Gemfinder!</h1>
<% @properties.each do |property| %>

<%= property.price %>
<%= property.address %>
<%= image_tag property.photo %>
<% end %>

    <div
      id="map"
      style="width: 100%;
      height: 720px;"
      data-markers="<%= @markers.to_json %>">
    </div>
